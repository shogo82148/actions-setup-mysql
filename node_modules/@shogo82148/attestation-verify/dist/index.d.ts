import { Result } from "./verification";
export interface VerifyOptions {
    algorithm?: string;
    githubToken?: string;
    owner?: string;
    repository?: string;
}
export declare function verify(filename: string, opts: VerifyOptions): Promise<Result>;
