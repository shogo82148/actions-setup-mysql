export type Bundle = {
    [key: string]: any;
};
export type Attestation = {
    bundle: Bundle;
};
export type AttestationsResponse = {
    attestations: Attestation[];
};
export interface ClientOptions {
    userAgent?: string;
    githubToken?: string;
}
export declare class Client {
    private httpClient;
    constructor(opts?: ClientOptions);
    getByRepoAndDigest(repo: string, digest: string): Promise<Attestation[]>;
    getByOwnerAndDigest(owner: string, digest: string): Promise<Attestation[]>;
    private get;
}
